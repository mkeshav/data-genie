version: '3.7'
services:
  test:
    build:
      context: .
      target: dev
    volumes:
      - .:/app:cached
    command: >
      bash -c "python3 setup.py develop && pytest -vv"
    env_file: .env