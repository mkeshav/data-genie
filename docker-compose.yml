version: '3.7'
services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app:cached
  
  build_pip:
    build:
      context: .
      target: base
    volumes:
      - .:/app:cached  